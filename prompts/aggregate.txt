## IMPORTANT Instructions
You will be given multiple agent reviews in Markdown files. Read each listed file from the workspace, then combine them into a single, high-quality PR review.

Input files:
{{AGENT_FILES}}
comments.md: PR comments captured from the GitHub API. Use this only to resolve ambiguity or conflicts.
pr_description.md: PR description (body) captured from the GitHub API. Use this only to resolve ambiguity or conflicts.

Task: Merge and de-duplicate findings. Resolve conflicts by prioritizing correctness, security, and production risk. Keep the review concise but complete.
Output: Respond with only the final review body in Markdown in {{LANGUAGE}}, specifying exact line number ranges within the new hunks.
        The final review must include:
        1) Explain what this PR does
        2) Code review and suggestions for improvement
        3) Detect bugs and potential bugs
        4) Ensure every issue includes a severity tag (P0/P1/P2/P3) using:
           - P0: 서비스 중단/데이터 손실/보안 치명
           - P1: 핵심 기능 장애/심각한 오류
           - P2: 우회 가능/중간 수준 문제
           - P3: 경미한 개선/스타일/권장사항
        Follow the below rules.
        - Blocking severity guard:
          - P0, P1, and P2 require a concrete incorrect observable behavior introduced or exposed by the PR. State the trigger, affected execution path, and incorrect outcome; if those cannot be established from the agent reviews and verified repository context, downgrade to P3 or omit the finding.
          - A finding that says the current behavior is correct, no leak or failure exists, risk is low, or merely asks to consider readability, type hints, comments, naming, defensive cleanup, or micro-optimization must not be P0, P1, or P2.
          - Treat PR descriptions and comments as context, not proof. However, before contradicting an explicit authoritative contract or fail-closed constraint recorded there, verify the conflicting repository behavior and cite the concrete code path. Do not recommend widening accepted inputs or skipping invalid data merely to bypass that contract.
          - Before returning the review, self-audit every P0-P2 finding against this guard and downgrade or remove any finding that lacks a reproducible correctness, security, data-integrity, or production-availability impact.
        - Do not use any fenced code blocks (```).
        - Do not use `diff` code blocks, `suggestion` blocks, or any block that contains raw code. All fix suggestions must be provided in plain natural-language descriptions.
        - Do not annotate code snippets themselves with line numbers. Ensure code is properly formatted and indented when referenced verbally.
        - Use only the line number ranges referenced in the agent reviews; do not invent new line numbers.
        - Do not ask the user to provide files, permissions, or additional access. The listed files are available in the current workspace and must be read directly.
        - Do not mention file access limitations unless a file is genuinely missing from the workspace after you check it.

Custom instructions (optional):
{{CUSTOM_INSTRUCTIONS}}

## MUST FOLLOW
Return only the Markdown review body in {{LANGUAGE}} with no preface about saving files.
